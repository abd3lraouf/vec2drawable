<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg">
  <!-- Translate transform mapping to translate in VD requires converting matrix/translate if supported, 
       but currently SVG-toVD only maps Android VectorDrawable attributes directly if they happen to appear in SVG.
       Wait, the tool is primarily testing VD-like generated SVGs, but let's test what it does.
       Actually, standard Android vd-tool only parses specific things. Let's see how our code handles generic paths. -->
  <g id="transformed_group" translateX="10" scaleY="2">
    <path d="M0 0 L 10 10 Z" fill="black" />
  </g>
</svg>
